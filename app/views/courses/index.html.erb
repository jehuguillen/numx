<p>courses/index.html.erb y al precionar "Calculus" me redirecciona a courses/show.thml.erb</p>
<div class="row">
	<div class="col-md-12">
	<%= link_to 'New Course', new_course_path, class: 'btn btn-primary btn-sm'%></td>

<div class="row">
	<div class="col-md-12">
	<table class="table">
		<thead>
			<th>Name</th>
			<th>Code</th>
			<th></th>
		</thead>
		<tbody>
			
			<% @courses.each do |c| %>
			<tr>
				<td><%= link_to c.name, course_path(c) %></td>
				<td><%= c.code%></td>
				<td><%= link_to 'X', course_path(c), method: :delete, class: 'btn btn-danger btn-sm'%></td>
			</tr>
			<% end %>
		</tbody>
	</table>
		
	</div>	
</div>
